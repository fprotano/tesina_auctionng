import { HelpCenterThreadDTO } from './help-center-thread-dto';

describe('HelpCenterThreadDTO', () => {
  it('should create an instance', () => {
    expect(new HelpCenterThreadDTO()).toBeTruthy();
  });
});
